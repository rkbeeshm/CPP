#ifndef ellipse_HPP
#define ellipse_HPP

class ellipse{
    public: 
        // Declare a function that takes input   
        float ellipse_area(float, float);
    
    private:
        // Variables to store input values
        float SMaj_axis, SMin_axis;
};

#endif