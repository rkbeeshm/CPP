#ifndef triangle_HPP
#define triangle_HPP

class triangle{
    public: 
        // Declare a function that takes input   
        float triangle_area(float, float);
    
    private:
        // Variables to store input values
        float base, height;
};

#endif